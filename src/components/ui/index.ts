/**
 * FOREAS UI v2 — Barrel exports
 *
 * Mirror du barrel `@/components/common` de l'app (skill §9).
 * Importer toujours depuis ce barrel, jamais de fichier direct.
 *
 * Usage :
 *   import { Button, Card, FoIcon } from '@/components/ui'
 *
 * Tokens :
 *   import { PALETTE, TYPO, SPACE, RADIUS, MOTION } from '@/design/tokens'
 */

export { Button, type ButtonVariant, type ButtonSize } from './Button'
export { Card, type CardVariant, type CardGlow, type CardRadius, type CardProps } from './Card'
export { FoIcon, type FoIconName, type FoIconProps } from './FoIcon'
